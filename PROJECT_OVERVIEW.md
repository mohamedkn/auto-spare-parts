# نظرة عامة على المشروع: "زي" - منصة متعددة المتاجر (Marketplace)

هذا الملف يوثق الهيكلة الكاملة للمشروع، ما تم إنجازه حتى الآن، وكيف تترابط أجزاء النظام مع بعضها البعض، بناءً على الخطة الأساسية (`PLAN.md`).

---

## 1. التقنيات المستخدمة (Tech Stack)

*   **الإطار الأساسي (Framework):** Next.js 14+ (باستخدام App Router) مع TypeScript.
*   **قاعدة البيانات (Database):** PostgreSQL.
*   **إدارة البيانات (ORM):** Prisma.
*   **التصميم والواجهات (UI):** Tailwind CSS + مكونات جاهزة من shadcn/ui.
*   **المصادقة (Authentication):** نظام مصادقة يعتمد على الـ Tokens / الجلسات (مع دعم الأدوار: Customer, Vendor, Admin).

---

## 2. ما تم إنجازه فعلياً في المشروع (Progress)

من خلال مراجعة ملفات المشروع، تم بناء الهيكل الأساسي وتطوير العديد من الميزات التي تغطي المراحل الأولى والثانية:

### أ. قاعدة البيانات (Prisma Schema v3)
تم بناء هيكل قوي ومحكم لقاعدة البيانات يتضمن الجداول التالية مع إعداد القيود (Constraints) و(Indexes):
*   `User`: لإدارة المستخدمين بجميع أدوارهم.
*   `Vendor`: بيانات المتاجر المربوطة بالمستخدمين (علاقة 1-1).
*   `Category` & `Product` & `ProductVariant`: لإدارة التصنيفات والمنتجات وخصائصها.
*   `Cart` & `CartItem`: عربات التسوق للمستخدمين.
*   `Order` & `SubOrder` & `OrderItem`: نظام الطلبات المتقدم الذي يقسم الطلب الرئيسي إلى طلبات فرعية حسب التاجر.
*   `Payment` & `Payout` & `Refund`: لإدارة المعاملات المالية والتسويات.
*   إضافات المرحلة 2.5: `WishlistItem` (المفضلة)، و `UserAddress` (عناوين الشحن).

### ب. مسارات الواجهة الخلفية (API Routes في `src/app/api`)
تم إنشاء مسارات API شاملة للتحكم في كافة العمليات:
*   **المصادقة:** `/api/auth`
*   **لوحة تحكم الإدارة:** `/api/admin` (الموافقة على المتاجر، إدارة العمولات، إلخ).
*   **لوحة تحكم البائع:** `/api/vendor` (إدارة منتجات التاجر، متابعة طلباته، التسويات المالية).
*   **المنتجات والتصنيفات:** `/api/products` و `/api/categories`.
*   **العميل (الواجهة الأمامية):** `/api/cart` (السلة)، `/api/checkout` (إتمام الطلب)، `/api/orders` (متابعة الطلبات).
*   **ميزات إضافية:** `/api/addresses` (العناوين)، `/api/wishlist` (المفضلة)، و `/api/webhooks` (لربط بوابات الدفع/الأحداث الخارجية).

### ج. واجهات المستخدم (Pages & UI)
تم بناء المجلدات لصفحات الواجهة في `src/app`:
*   `admin/`: صفحات تحكم الإدارة.
*   `vendor/`: لوحة تحكم التجار (Dashboard).
*   `products/`: عرض المنتجات للعملاء.
*   `orders/` و `profile/` و `wishlist/`: صفحات خاصة بالعملاء لمتابعة نشاطهم.
*   **المكونات (Components):** تم تنظيمها في `src/components` (أجزاء مشتركة، ومكونات UI الأساسية، ومكونات المنتجات).
*   **الأدوات المساعدة (Lib):** مجلد `src/lib` يحتوي على خدمات المصادقة (`auth`)، قواعد البيانات (`db.ts`)، الدفع (`payments`)، خدمات معالجة البيانات (`services`)، والتحقق (`validations`).

---

## 3. هيكلة العمل وكيفية ترابط الأجزاء (Architecture & Data Flow)

يعتمد المشروع على بنية **متعددة المتاجر** حيث يشتري العميل منتجات من عدة تجار في نفس الطلب. إليك كيف يعمل النظام:

### 1. رحلة التاجر (Vendor Flow)
1.  **التسجيل:** يسجل المستخدم حساباً ويطلب تحويله إلى بائع (`vendor`).
2.  **الموافقة:** يقوم الـ Admin بمراجعة الطلب (تتغير الحالة من `pending` إلى `approved` في جدول `vendors`).
3.  **إدارة المنتجات:** يدخل البائع إلى `/vendor` ويضيف منتجاته (`Product`)، والتي ترتبط تلقائياً بالـ `Vendor ID` الخاص به.
4.  **استقبال الطلبات:** عندما يشتري عميل منتجاً، يرى التاجر فقط الـ `SubOrder` (الطلب الفرعي) الخاص به، ولا يرى منتجات التجار الآخرين في نفس الطلب.
5.  **الأرباح والتسويات:** بعد إتمام الطلب، يتم حساب أرباح التاجر بعد خصم عمولة المنصة (`commissionRate`)، وتضاف إلى سجل الـ `Payouts` ليتم تسديدها له لاحقاً.

### 2. رحلة العميل (Customer Flow)
1.  **التصفح:** يتصفح العميل المنتجات (`/products`) ويمكنه إضافتها إلى المفضلة (`/wishlist`).
2.  **السلة:** يضيف العميل المنتجات إلى السلة (`Cart`).
3.  **إتمام الطلب (Checkout):** 
    *   عند الدفع، يتم إنشاء **طلب رئيسي (`Order`)** واحد للعميل ليدفع مبلغاً إجمالياً واحداً.
    *   **في الخلفية (سحر النظام):** يقوم النظام بتقسيم هذا الطلب إلى **طلبات فرعية (`SubOrders`)** بناءً على المتاجر التي تم الشراء منها.
4.  **التتبع:** يتتبع العميل حالة كل جزء من الطلب بشكل منفصل (لأن كل تاجر يشحن منتجاته بشكل مستقل).

### 3. رحلة الإدارة (Admin Flow)
*   يمتلك الأدمن صلاحية رؤية كل شيء في النظام.
*   يتحكم في التصنيفات (`Categories`).
*   يراقب المتاجر، يحدد نسبة العمولة (`Commission Rate`) لكل متجر.
*   يدير النزاعات (`Disputes`) والطلبات المرتجعة.

### 4. الترابط المعماري في قاعدة البيانات (Entity Relationships)
*   **`User` (Role: Vendor) <--> `Vendor`**: علاقة ارتباط فريدة (كل حساب تاجر يملك متجر واحد فقط).
*   **`Vendor` <--> `Product`**: كل متجر يمكنه نشر عدة منتجات. يتم حذف منتجات المتجر تلقائياً إذا تم حذف المتجر (`CASCADE`).
*   **`Order` <--> `SubOrder`**: الطلب الرئيسي يحتوي على عدة طلبات فرعية (لكل متجر طلب فرعي). يتم ربط الـ `SubOrder` بالـ `Vendor`.
*   **`SubOrder` <--> `OrderItem`**: المنتجات المحددة التي اشتراها العميل من هذا التاجر.
*   **`Optimistic Locking`:** تم إضافة حقل `version` في جدول `Product` لضمان عدم بيع كمية غير متوفرة (Overselling) إذا قام شخصان بالشراء في نفس اللحظة.

---
**الخلاصة:**
المشروع مبني بشكل احترافي جداً كمنصة Marketplace حقيقية (مثل أمازون أو نون). الأساسيات (Phase 1 & 2) تم وضعها بالكامل من خلال مخطط قاعدة البيانات القوي، مجلدات الـ API الجاهزة للاستخدام، ومسارات واجهة المستخدم المنظمة بناءً على صلاحيات كل دور (Customer, Vendor, Admin).
